VITE_API_BASE_URL=http://localhost:4000/api
# Serve every screen from local fixtures instead of apps/api. Lets the
# dashboard be built and demoed before the backend routes exist.
VITE_USE_FIXTURES=true
